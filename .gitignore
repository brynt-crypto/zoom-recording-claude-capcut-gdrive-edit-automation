# Secrets / machine-local config — never commit
.env
.env.*
!.env.example

# OS / editor junk
.DS_Store

# Machine-specific pinned requirements (keep only the shared requirements.txt)
requirements-*.txt

.venv/
.venv-mac/
jobs/
work/
sandbox_drafts/
outputs/
assets/style_ref/
__pycache__/
*.pyc
.pytest_cache/

# Raw-media drop folder (large source videos — not source-controlled)
assets/to_edit/
# Drive pipeline runtime ledger (per-machine state)
drive_sync/state.json
